<fieldset>
	<legend>show.html.erb</legend>
	<h1>Listing majors</h1>

	<table>
	  <tr>
	    <th>Major</th>
	    <th></th>
	    <th></th>
	    <th></th>
	  </tr>

	<% @majors.each do |major| %>
	  <tr>
	    <td><%= major.major %></td>
	    <td><%= link_to 'Show', major %></td>
	    <td><%= link_to 'Edit', edit_major_path(major) %></td>
	    <td><%= link_to 'Destroy', major, method: :delete, data: { confirm: 'Are you sure?' } %></td>
	  </tr>
	<% end %>
	</table>

	<select>
		<% @majors.each do |major| %>
			<option><%= major.major %></option> 
		<% end %>
	</select>

	<br />

	<%= link_to 'New Major', new_major_path %>

	<p style="background:green; width:25em;">/app/views/majors/index.html.erb</p>
</fieldset>
